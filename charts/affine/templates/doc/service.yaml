apiVersion: v1
kind: Service
metadata:
  name: {{ include "doc.fullname" . }}
  labels:
    {{- include "doc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.doc.service.type }}
  ports:
    - port: {{ .Values.doc.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.doc.service.nodePort }}
    - port: {{ .Values.doc.service.nodePort }}
      targetPort: http
      protocol: TCP
      name: node-port
    {{- end }}
  selector:
    {{- include "doc.selectorLabels" . | nindent 4 }}
