apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.front.services.renderer.name }}
  labels:
    {{- include "front.labels" . | nindent 4 }}
  {{- with .Values.front.services.renderer.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.front.services.renderer.type }}
  ports:
    - port: {{ .Values.front.services.renderer.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "front.selectorLabels" . | nindent 4 }}
