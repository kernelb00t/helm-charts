apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "graphql.fullname" . }}-test-connection"
  labels:
    {{- include "graphql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: "{{ .Values.global.imageRegistry }}/busybox"
      imagePullPolicy: IfNotPresent
      command: ['wget']
      args: ['{{ include "graphql.fullname" . }}:{{ .Values.graphql.service.port }}']
  restartPolicy: Never
