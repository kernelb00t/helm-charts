{{- if and ( .Capabilities.APIVersions.Has "monitoring.coreos.com/v1" ) ( .Values.serviceMonitor.enabled ) }}
{{- if and (eq .Values.db.type "postgresdb") (eq .Values.worker.mode "queue") }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
{{- if .Values.serviceMonitor.labels }}
  labels:
{{ toYaml .Values.serviceMonitor.labels | indent 4}}
{{- end }}
  name: {{ include "n8n.names.fullname" . }}-worker
{{- if eq .Values.serviceMonitor.namespace "" }}
  namespace: {{ .Release.Namespace }}
{{- else }}
  namespace: {{ .Values.serviceMonitor.namespace }}
{{- end }}
spec:
  podMetricsEndpoints:
    - port: {{ .Values.global.service.name }}
    {{- if .Values.serviceMonitor.interval }}
      interval: {{ .Values.serviceMonitor.interval }}
    {{- end }}
      path: /metrics
    {{- if .Values.serviceMonitor.timeout }}
      scrapeTimeout: {{ .Values.serviceMonitor.timeout }}
    {{- end }}
    {{- if .Values.serviceMonitor.metricRelabelings }}
      metricRelabelings:
{{ toYaml .Values.serviceMonitor.metricRelabelings | indent 8 }}
    {{- end }}
      relabelings:
        - sourceLabels: [__meta_kubernetes_pod_name]
          targetLabel: pod_name
        - action: replace
          targetLabel: role
          replacement: "worker"
  {{- if ne .Values.serviceMonitor.namespace .Release.Namespace }}
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "n8n.labels.matchLabelsComponent" (dict "context" . "component" "worker") | nindent 6 }}
{{- if .Values.serviceMonitor.targetLabels }}
  podTargetLabels:
  {{- range .Values.serviceMonitor.targetLabels }}
      - {{ . }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
